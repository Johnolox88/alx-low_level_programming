#include "main.h"
#include <stdio.h>

/**
 * reverse_array - reverses arr[] from start to end
 * @a: integer
 * @n: integer
 */

void reverse_array(int *a, int n)
{
	int temp;

	while (start < end)
	{
		temp = arr[start];

		arr[start] = arr[end];

		arr[end] = temp;

		start++;

		end--;
	}
}
