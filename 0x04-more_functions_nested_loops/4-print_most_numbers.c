#include "main.h"
#include <ctype.h>

/**
 * print_most_numbers(void) - prints numbers from 0 to 9 without printing 2 and 4
 *
 * Return: 1 if number is between 0 and 9 but is not 2 or 4 else return 0
 *
 * @c: is an integer
 */

void print_most_numbers(void);
{
	int i = print_numbers(void);
	if (i > 0)
	{
		return (1);
	}
	else
	{
		return (0);
	}

}
