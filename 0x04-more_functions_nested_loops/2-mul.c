#include "main.h"
#include <ctype.h>

/**
 * mul - multiplies two integers
 *
 * Return: 1 if a, b are integers and 0 if otherwise
 *
 * @a, b: are integers
 */

int mul(int a, int b)
{
	int i = mul(int a, int b);

	if (i > 0)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
