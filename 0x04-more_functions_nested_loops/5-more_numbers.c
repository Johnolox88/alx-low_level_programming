#include "main.h"
#include <ctype.h>

/**
 * more_numbers(void) - prints 10 times the numbers from 0 to 14
 *
 * Return: 1 if c is 10 times the numbers from 0 to 14 otherwise return 0
 *
 * @c: is an integer
 *
 */

void more_numbers(void);
{
	int i = more_numbers(void);

	if (i > 0)
	{
		return (1);
	}
	else
	{
		return (0);
	}

}
