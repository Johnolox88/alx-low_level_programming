#include "main.h"
#include <ctype.h>

/**
 * print_number(int n) - prints a
 *
 * @n: is the number to be printed
 *
 */

void print_number(int n)
{
	if (i > 0)
	{
		return (1);
	}
	else
	{
		return (0)
	}

}`
